# このリポジトリでの作業方針

## 返答は日本語で行う
ユーザーとのチャットでの返答は、常に日本語で行うこと。
コード中のコメント・commitメッセージ・ドキュメント類も日本語でよい。

## 設計ドキュメントの場所
実装前に必ず以下を参照すること。矛盾する実装をしないこと。
- `docs/requirements.md` … 要件定義（背景・スコープ・機能一覧・データモデル・受け入れ条件）
- `docs/screens.md` … 画面構成・情報設計（画面一覧・遷移図）
- `docs/firestore-design.md` … Firestoreのコレクション構造・セキュリティ方針
- `docs/ROADMAP.md` … 実装タスクの一覧・進行状況

## 手動でのやり取り(evolveサイクル外)では、明記されない限り実装しない
`/evolve`のcron自動起動サイクル以外で、ユーザーと直接チャットでやり取りしている間
(設計相談・感想・「〜だったらよかった」のような振り返りコメントなど)は、
**ユーザーが明示的に「実装して」「進めて」等と指示するまでコードの実装をしないこと**。

- 設計についての意見・気づき・改善案が出た場合は、まず`docs/ROADMAP.md`(該当する
  `### N`セクション、または新規タスクとして)に書き留めるか、口頭で認識を
  合わせるだけに留める。ファイルの編集・新規作成はしない。
- 「タスクに入れて」と言われた場合、それは`docs/ROADMAP.md`へのタスク追記を指す
  (Claude Code内蔵のTaskCreate/TodoWriteツールのことではない)。
- 実装してよいか迷う場合は、実装せずに一言確認してから進める。
- この方針は`evolve` skill(cron起動の自動サイクル)には適用されない。
  `/evolve`が正規のサイクルとして起動された際は、通常どおり
  `.claude/skills/evolve/SKILL.md`の手順に従って実装・テスト・commit・pushを行う。

## データモデルを厳守する
`docs/firestore-design.md` に定義したコレクション構造・フィールド名から逸脱しないこと。
構造を変えたくなった場合は、実装せず`docs/firestore-design.md`の変更案として
提案するに留め、人間の承認を待つ。

## セキュリティルールを緩めない
`docs/firestore-design.md`の「セキュリティ方針」に定めた対策(list禁止・合言葉桁数・
App Check)を、実装の都合で緩める(例: listを許可してしまう等)ことは絶対にしないこと。
