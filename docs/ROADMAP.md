# ROADMAP

## 進め方の方針
- タスクはこの順で上から着手する: `0. 基盤` → Must機能(1〜6) → Should機能(7〜9)
- `0. 基盤`が完了するまで、他のセクションには着手しない(全機能がFirestore接続と
  画面シェルに依存するため)
- 1サイクル = 意味的にまとまった単位(目安: S/Mサイズ1〜3個)。詳細な1周の手順は
  `.claude/skills/evolve/SKILL.md`を参照
- サイズ目安: S = 数十行程度の変更、M = 1画面の主要機能一式

## バグ修正(最優先 — 通常タスクより先に上から順に着手する)
2026-08-19、人間との会話で脆弱性の懸念を洗い出し、タスク化した3件は全て完了した
(詳細はdocs/roadmap-done.md参照)。`firestore.rules`の`groups/{groupCode}`の
`update`制限は、Claude Codeの自動モード安全装置により自動デプロイがブロックされたため
人間が手動で`npm run firebase:deploy:rules`を実行し、本番環境(`trip-planner-cd9b7`)へ
反映済み(2026-08-19)。デプロイ後、許可外フィールドへのupdateが`permission-denied`で
拒否されること・`members`フィールドへの正規の追記(参加フロー)は引き続き成功することを
実Firestoreで確認済み。

`76. 雑多メモ固定バーがキーボード表示中に選択後のボタンを塞ぐ不具合`・
`81. --shadow-buttonの青み残りを暖色化`・`82. フォーカスリングのハードコード解消`は
いずれも修正完了(詳細はdocs/roadmap-done.md参照)。

(現在、他に未対応のバグ無し)

## 新規タスク・画面提案(承認待ち)
新しい画面の追加提案、または既存画面構成からの変更提案はここに書くこと。
実装には進まない。

(現在なし。「A. リアルタイム同期」は2026-08-19、`第8期`としてS/Mに分割済み・
着手可能な状態のため下の「タスク一覧と進行状況」へ移動した)

(2026-08-17: セキュリティ方針(list許可・削除権限)の変更提案は、
`docs/firestore-design.md`の更新と人間の明示的な指示により確定・実装・デプロイ済み。
詳細はcycle-log参照)

---

## タスク一覧と進行状況

`0. 基盤`〜`9. 仕上げ`まで、旧ROADMAP上のタスクはすべて完了した(2026-08-18)。
完了済みタスクの詳細は`docs/roadmap-done.md`を参照。

---

## 第2期: UI刷新(SPA化・企画メモ共有化・雑多メモ機能)

利用してみた人間からのフィードバックを受け、`docs/screens.md`「UI刷新方針」・
`docs/firestore-design.md`「UI刷新に伴うデータモデル変更」の通り2026-08-18に方針決定。
詳細な経緯・設計判断は両ドキュメントを参照。

`10`〜`16`(基盤SPA化・A/B画面移行・Cタブ構造化・企画メモ共有テキスト化・雑多メモ機能・
しおり時間UI変更・見た目の刷新)は全て完了済み(詳細はdocs/roadmap-done.md参照)。
旧MPA版(`pages/index.html`等)は削除済み。`index.html`(旧`app.html`)がプロジェクトルート
の唯一のエントリーポイント。企画メモ・雑多メモはそれぞれ`trips/{tripId}.planningNotesText`
・`scratchText`(単一共有テキスト、デバウンス自動保存)。雑多メモは選択範囲を
「→企画メモへ」「→しおりへ」ボタンで振り分け可能。しおりの時間入力は「午前/午後」
「時(0〜12)」「分(00/15/30/45)」の3セレクトボックス(保存形式は従来通り"HH:MM"文字列)。
配色はtabiori.com参考の明るい青×オレンジ基調に刷新済み(`styles/tokens.css`)。

第2期のタスクは以上で全て完了。

---

## 第3期: レイアウト刷新

`16`でトークン(配色・角丸等)は変更したが、人間から「レイアウトも汚い」との評価を受け、
2026-08-19に人間とのチャットで追加方針を決定。トークンいじりだけでなく、各画面のHTML
構造・CSSを作り直すレベルまで踏み込む(データモデル・Firestore構造・`docs/screens.md`の
画面構成・遷移は不変。あくまで既存画面の内部レイアウト・視覚表現の作り直し)。

背景・調査結果:
- 「安っぽく見える」の主因は、絵文字的な装飾の不足ではなく、①SVGアイコンが無い
  ②タイポグラフィに階層が無い ③余白のリズムが均一 ④画面ごとの表情が全部同じ
  (どのタブも「見出し→フォーム→リスト」の判で押した構成) ⑤状態変化にトランジションが
  無く一瞬で切り替わる、の5点と分析した。
- Wanderlog(旅行プランナー)・TimeTree(共有カレンダー)等の既存アプリを調査し、
  「追加フォームを常時表示せず折りたたむ」「一覧をカード/タイムライン的に見せる」等の
  パターンを参考にした。絵文字によるアイコン代用は不採用(OS/ブラウザで見た目が
  バラバラになり、狙いと逆に安っぽく見えるリスクがあるため)。

`17. 共通基盤の整備`・`18. 画面ごとのレイアウト刷新`は全て完了済み
(詳細はdocs/roadmap-done.md参照)。トークン・タイポグラフィ・カード/ボタンの質感刷新、
SVGアイコンセット(`src/icons.js`)、折りたたみフォームパターン(E〜Hの追加フォーム)、
A(参加)のカード分離、B(旅行一覧)のカード情報拡充、C(概要)の表示/編集モード統一、
D・雑多メモのカード化、E(行き先決め)のランキング視覚化(順位バッジ+スコアバー)、
G(宿泊)の候補/確定宿泊の区分、H(しおり)のタイムライン風表示まで全て完了。

第3期のタスクは以上で全て完了。

---

## 第4期: PC(広い画面)向けレスポンシブ対応

`docs/requirements.md`「プラットフォーム: モバイルブラウザ中心、レスポンシブ対応」を
満たせていない点として、2026-08-19に人間から指摘。現状`.page { max-width: 480px; }`が
画面幅によらず常に固定されており、PCの広い画面では左右に余白ができて物足りない見た目に
なっていた。人間との相談の結果、**ウィンドウ幅に応じて動的に、広い画面ではカード一覧を
複数カラムのグリッド表示にする方式**で確定(単純な固定幅拡大ではなく、レスポンシブな
複数カラムレイアウトを採用)。データモデル・Firestore構造・`docs/screens.md`の画面構成・
遷移は不変。あくまでCSS(メディアクエリ)による表示側の対応。

`19. 共通基盤: ブレークポイント・幅の可変化`・`20. リスト系画面のグリッド化`・
`21. その他画面の広い画面での調整`は全て完了済み(詳細はdocs/roadmap-done.md参照)。
`.page`のmax-widthが768px/1024pxで段階的に拡大し、再利用可能な`.card-grid`共通クラス
(768px以上で2列、1024px以上で3列)をB・E・F・G(候補・確定宿泊とも)に適用。C・雑多メモ・
企画メモ・Hはグリッド化せず`19`の幅拡大のみで問題ないことを確認済み。

第4期のタスクは以上で全て完了。

---

`22. 雑多メモの振り分け先を拡張`は完了済み(詳細はdocs/roadmap-done.md参照)。
雑多メモの選択範囲振り分けボタンは「→企画メモへ」「→行き先決めへ」「→しおりへ」
「→宿泊へ」の4つになった(日程調整は対象外)。

---

## 第5期: 配色・タイポグラフィの統一感強化

2026-08-19、人間から「デザインがまだ単調」との指摘を受け、複数の参考サイト
(sankoudesign.com「旅行・観光」カテゴリ、w4serinn.github.io/yamagata_trip、
miricanvas「旅のしおり」テンプレート、design-library.jp「お問い合わせ」UI集、
g-mark.org受賞ギャラリー)を調査した上で方針を確定。「和風」等の特定スタイルへの
転換ではなく、**既存の明るい青ベース配色を活かしつつ、体系立った統一感を持たせる**
方向で合意(配色の全取っ替えは4回目になるため、既存資産を活かす)。

背景・調査結果:
- yamagata_tripの実CSSを直接確認したところ、フォントペアリング・伝統色による
  体系的な配色・CSSのみのテクスチャ(画像不使用)・番号バッジ・全面塗りセクションに
  よるリズムづけが、写真無しでも「単調に見えない」ことに効いていると判明
- design-library.jp自体のCSSから、ピル型ボタン(`border-radius:50px`、
  パディング`9-10px 14-16px`)・青みを帯びたカード影(`rgba(0,0,128,...)`系)の
  具体的な数値を採取
- miricanvasの「旅のしおり」テンプレートは「ベース+アクセント2〜3色に絞る」
  「情報をブロックごとに枠線で区切る」という、既存のカード主体の設計と親和性の高い
  方針を採用している
- g-mark.orgの受賞デザイン(id:14776)は「AIカレンダー/日程調整ツール」という
  同カテゴリの製品だったが、JS描画のため詳細は自動取得できず、人間が直接確認したい
  場合は別途ブラウザで見る必要がある

`23. デザイントークンの拡充`は完了済み(詳細はdocs/roadmap-done.md参照)。
Zen Kaku Gothic New採用・`--color-primary-deep`/`--color-accent-green`追加・
青みを帯びた`--shadow-card`・`--radius-pill`追加と`.rank-badge`への適用まで完了。

`24. ヘッダーのテクスチャ強化`は完了済み(詳細はdocs/roadmap-done.md参照)。
`.page-header`に`--color-primary-deep`系グラデーション+ドット柄パターンを適用し、
見出し・戻るリンクの文字色を白系に変更した。

`25. しおりタイムラインの装飾強化`は完了済み(詳細はdocs/roadmap-done.md参照)。
タイムラインマーカーを丸ドットから、その日の何番目の予定かを示す連番バッジに変更した。

`26. コンテンツ内のダーク全面塗りセクション追加`は完了済み(詳細はdocs/roadmap-done.md
参照)。雑多メモタブの`<textarea>`を囲む`.card`(`.scratch-card`)に、ヘッダーと同系統の
濃色グラデーション+ドット柄・明るい文字色を適用した。

### 27. カスタムカレンダーピッカーの導入(複数選択対応)
2026-08-19、jicoo.com(日程調整ツール)を参考に人間から提案。現状の`<input type="date">`は
ブラウザ標準のUIで、見た目に一貫性が無い。F(日程調整)画面から着手し、独自にスタイリングした
カレンダー形式の日付選択コンポーネントに置き換える(格子状のピル型ボタンで日付/時間帯を
選ぶ、というjicoo.comのUIパターンを参考にしつつ、このアプリの実際のデータ構造に合わせて
調整する)。加えて人間から、**F画面では複数日をまとめて選択・追加できるようにして操作性を
上げてほしい**との要望あり(候補日を1件ずつ「開く→選ぶ→送信」を繰り返す手間を無くす)。
保存するデータ形式(`scheduleEntries/{date}`、1ドキュメント=1日付という構造)自体は
変えない(しおりの時間入力をプルダウン化した`15`と同じ考え方。複数選択時も、選択された
日付ごとに`setDocumentMerged`を呼ぶだけで、Firestoreスキーマ・書き込み方式は据え置き)。
CSSだけでは完結せず、月めくり・日付グリッド描画・複数選択状態の管理等のJSロジックが
新たに必要になる規模のため、Lサイズとして以下に分割する。
`27. カスタムカレンダーピッカーの導入`は完了済み(詳細はdocs/roadmap-done.md参照)。
`src/datePicker.js`(単一・複数選択モード対応)を新規作成し、F(日程調整)の「候補日を
追加」(複数選択モード)、G(宿泊)の確定宿泊チェックイン/チェックアウト・H(しおり)の
日付(いずれも単一選択モード)に適用した。既存の`<input type="date">`はF/G/Hから
すべて置き換わった。

第5期のタスクは以上で全て完了。

---

## 第6期: 第5期リリース後の見た目フィードバック対応

2026-08-19、第5期完成直後に人間から2件の指摘。

`28. カレンダーピッカーをコンパクトにする`は完了済み(詳細はdocs/roadmap-done.md参照)。
`.date-picker`にmax-width、`.date-picker-day`にmax-width(32px)+`margin: 0 auto`を
設定し、画面幅によらず日付セルが32px×32pxの固定サイズになるよう調整した。

`29. 雑多メモだけ背景色があり企画メモは無い、という非対称さの見直し`は完了済み
(詳細はdocs/roadmap-done.md参照)。`.scratch-card`を汎用的な`.card-dark`に改称し、
企画メモ(`src/views/notes.js`)にも同じダーク全面塗りを適用して見た目を揃えた。

第6期のタスクは以上で全て完了。

---

## 第7期: アプリ全体のデザイン一新(構造・レイアウトのみ。配色は変更しない)

2026-08-19、`28`(カレンダーピッカーのコンパクト化)の相談の中で、人間から
「このアプリ全体のデザインを一新したい、Notionが近い」との意向表明。人間から明確な
訂正あり: **「カードの配置とかサイドバーとかそういうデザイン的なつくりを寄せるので
あって、配色は変えなくていい」** — Notionを参考にするのは配色ではなく、構造・
レイアウト面(情報の並べ方・ナビゲーションの形)であると明確化された。
`styles/tokens.css`の色トークン(`--color-*`)は一切変更しない。第5期で確定した
明るい青×コーラルオレンジ基調・`24`/`26`/`29`のダーク全面塗り等はそのまま維持する。

人間との確認により方向性確定。**サイドバー化は広い画面(768px以上)限定**、
**モバイル(768px未満)は現状の横タブバーの代わりにハンバーガーメニュー
(タップで展開するドロワー/オーバーレイ)**にする。サイドバー・ドロワーどちらも
現行タブバーと同じ7項目(概要・雑多メモ・企画メモ・行き先決め・日程調整・宿泊・
しおり)を並べる。`docs/screens.md`「画面遷移」のタブ=ルート対応は変えない
(ナビゲーションの見た目・配置のみの変更)。方向性が固まったため、evolveサイクルは
このタスクに着手してよい。

`31. サイドバー(広い画面)・ハンバーガーメニュー(モバイル)への刷新`は完了済み
(詳細はdocs/roadmap-done.md参照)。768px以上は常設サイドバー、768px未満は
ハンバーガーボタン+開閉ドロワーに統一し、横タブバー(`.tabbar`/`.tab`)は撤去した。
キーボード操作(ドロワー内でのTabフォーカス循環・Escでの閉じる)も対応済み。

第7期のタスクは以上で全て完了。

---

## 第8期: リアルタイム同期(onSnapshot化)

2026-08-19、人間との会話で「旅行の計画を立てるVC(通話)で使いやすいアプリ」という
前提で機能案を出し、好感触だったもの。現状の`src/firestore.js`は`getDoc`/`getDocs`
(一回きりの取得)のみで、誰かが候補地追加・投票・回答をしても他の参加者の画面には
反映されない(タブを開き直すまで気づかない)。`onSnapshot`によるリアルタイム
リスナーへの切り替えで、複数人が同時に操作する使い方(VC前提)を支える。

保存するFirestoreのコレクション構造・フィールド名(`docs/firestore-design.md`)は
変えない。読み取り方式のみの変更。以下、影響範囲ごとにS/Mへ分割する。

`src/firestore.js`への購読用共通関数の追加は完了済み(詳細はdocs/roadmap-done.md
参照)。`subscribeToDocument(path, onData, onError)`・
`subscribeToCollection(collectionPath, onData, onError)`を追加し、どちらも
`unsubscribe`関数を返す。既存の`getDocument`/`listCollection`はそのまま残している。
雑多メモ・企画メモのリアルタイム化は完了済み(詳細はdocs/roadmap-done.md参照)。
自分が編集中(未保存の変更がある、またはフォーカス中)の間はリモートの更新で
上書きしないガードを実装した。
E(行き先決め)・F(日程調整)のリアルタイム化は完了済み(詳細はdocs/roadmap-done.md
参照)。既存の楽観的ローカル更新は撤去し、購読による再描画に一本化して
重複更新・ちらつきを避けた。
G(宿泊)・H(しおり)・C(概要)・B(旅行一覧)への展開要否の判断、および全画面での
`unsubscribe`確認は完了済み(詳細はdocs/roadmap-done.md参照)。判断結果:
G・Hは展開する(下記`32`)、C・Bは見送り。

### 32. G(宿泊)・H(しおり)のリアルタイム化
E・Fと同じ理由(複数人が同時に候補を追加・確認する使い方)で展開する。C(概要)は
編集頻度が低く明示的な「編集」ボタン経由の単発更新のため、B(旅行一覧)は一覧を
見る場面が同時編集と衝突しにくいため、どちらも今回は見送る(効果が薄いと判断)。
`32. G(宿泊)・H(しおり)のリアルタイム化`は完了済み(詳細はdocs/roadmap-done.md参照)。
`src/views/lodging.js`(宿泊候補・確定宿泊の2コレクション)・`src/views/itinerary.js`を
`subscribeToCollection`化し、楽観的ローカル更新は撤去した。

第8期のタスクは以上で全て完了。

---

## 第9期: 運用フィードバック(その2)

2026-08-19、第8期(リアルタイム同期)完成後の人間との会話で2件の指摘。

`33. 雑多メモ・企画メモのリモート更新ガードが過剰`・`34. 雑多メモの振り分けを
「カット」ではなく「コピー」にする`・`35. 雑多メモ「→しおりへ」の簡易フォームが
カスタムカレンダーピッカーになっていない`・`36. ハンバーガーメニューの開閉に
アニメーションを追加`・`37. タブ(画面)遷移にもアニメーションを追加`は完了済み
(詳細はdocs/roadmap-done.md参照)。

第9期のタスクは以上で全て完了。

---

## 第10期: 運用フィードバック(その3)

2026-08-20、しおり機能について人間から2件の指摘。

`38. 雑多メモ「→しおりへ」の簡易フォームで日付以外も入力できるようにする`・
`39. しおり項目の編集・削除機能を追加`は完了済み(詳細はdocs/roadmap-done.md参照)。

第10期のタスクは以上で全て完了。

---

## 第11期: 各タブへの改善アイデア(ブレインストーム)

2026-08-20、人間から「各タブにこういう機能・レイアウト・デザインにしたらいいん
じゃないかっていう案を出してタスクに入れておいて。各画面につき3個」との依頼を受け、
Claudeが既存の実装(各`src/views/*.js`)を踏まえてブレインストームし、画面ごとに3案
ずつ書き出した。その後、同日中の人間とのチャットで項目ごとのフィードバックを受け、
要不要の判断が付いたものを反映済み(不要と言われた項目は削除済み)。
なお人間から「(このブレインストームで)思っていたのは、実際に使うとなって話してた
時に『あれ、これ目的地までの移動手段を書く場所ないじゃん』みたいな、使ってみて
気づく不便な点を見つけてほしいイメージだった」との補足があったため、下に
「実利用シーンからの気づき」を追加した(そちらを参照)。
2026-08-20、Firestoreスキーマ変更を伴う項目(`58`・`61`・`62`・`64`、いずれも
「(承認済み)」表記)について人間から一括承認を得た。着手時は各項目の指示通り
`docs/firestore-design.md`(・`61`は`docs/requirements.md`も)へスキーマ変更を
反映してから実装すること。それ以外の項目に承認は不要だが、着手時は改めて
`docs/screens.md`・`docs/firestore-design.md`との整合を確認すること。
サイズ目安は暫定(着手時に実装しながら再見積もりしてよい)。

### C. 概要タブ
`40. D〜H各機能の件数サマリーカード`・`41. 集合情報・割り勘リンクの2カラム化`・
`42. 旅行名エリアの装飾強化`は完了済み(詳細はdocs/roadmap-done.md参照)。

### 雑多メモタブ
`44. 振り分けボタンの画面下部固定表示`は実装完了(詳細はdocs/roadmap-done.md参照)。
実機の仮想キーボード表示・iOS Safariのビューポート挙動はPlaywright(ヘッドレス
Chromium)では再現・検証できないため、evolveサイクルでは`visualViewport`の値を
JSで模擬した動作確認のみ行った。**人間による実機での最終確認がまだ済んでいない**
(見た目・押しやすさに違和感があれば調整のフィードバックを歓迎する)。

(`45`. `.card-dark`内のテキストエリアのフォーカス時に枠線・影を強調する案は、
2026-08-20確認したところ`pages/shared.css`の`.card-dark textarea:focus`
(`border-color: var(--color-surface); box-shadow: 0 0 0 3px rgb(255 255 255 / 20%);`)
としてすでに実装済みだったため、追加対応不要と判断し取り下げ済み)

### 雑多メモ・企画メモタブ共通

(`46`. 先頭が「#」「・」等の行を表示側だけ軽く強調する簡易装飾案は、2026-08-20の
実装検討で「雑多メモ・企画メモはどちらも常時編集可能な`<textarea>`で表示しており、
`textarea`の中身は構造上プレーンテキストしか描画できず、特定の行だけCSSで太字化する
ことは技術的に不可能」と判明したため保留とした。実現するには`65`の検討事項で
記録済みのものと同じ「閲覧モード(整形済み表示)⇔編集モード(textarea)」への
設計変更が必要になるため、その検討事項に統合し、単独タスクとしては取り下げた
(詳細は本ドキュメント末尾の検討事項リスト参照))

`48. 雑多メモ・企画メモの視覚的差別化`は完了済み(詳細はdocs/roadmap-done.md参照)。

### 日程調整タブ
`52. 候補日カレンダー俯瞰ビュー`・`53. 自分が未回答の候補日をハイライト`・
`54. 候補日全体への一括回答ショートカット`は完了済み(詳細はdocs/roadmap-done.md参照)。

### 宿泊タブ
(`55`. 宿泊候補カードへのOGP画像表示は不要と判断し取り下げ済み。2026-08-20、
人間から「代わりに置かれたリンクをクリックすると飛べる仕組みにはしてほしい」との
要望があったが、`src/views/lodging.js`の候補・確定宿泊のURLは`isSafeUrl()`で
検証した上ですでに`<a target="_blank">`としてクリック可能な状態で描画済みのため、
追加対応は不要と判断した。人間が実際に動作しない場面に遭遇した場合は、その状況を
添えて再度タスク化すること)
`56. 宿泊候補→確定宿泊のワンタップ変換`・`64. 宿泊候補と確定宿泊のつながりの追跡`は
完了済み(詳細はdocs/roadmap-done.md参照)。

`57. 確定宿泊が複数件ある場合のガントチャート風タイムライン表示`は完了済み
(詳細はdocs/roadmap-done.md参照)。

### しおりタブ
`58. 同じ日の中で時間未設定の項目が複数あるときの手動並び替え`(ドラッグ&ドロップの
代わりに▲/▼ボタンで実装。モバイル中心という要件との兼ね合いによる判断。詳細は
docs/roadmap-done.md参照)・`59. 日付見出しのアコーディオン化`・
`60. 「次の予定」の強調表示`は完了済み(詳細はdocs/roadmap-done.md参照)。

### 実利用シーンからの気づき(2026-08-20追加)
上記のブレインストームに対し人間から「機能・レイアウトの思いつきというより、実際に
使うとなって話してた時に『あれ、これ目的地までの移動手段を書く場所ないじゃん』
みたいな、使ってみて気づく不便な点を見つけてほしかった」との補足があったため、
グループ旅行を実際に企画する流れ(`docs/requirements.md`4章の①企画→②行き先決め→
③宿→④しおり→⑤移動手段)を一つずつ辿り直し、既存の各`src/views/*.js`の入力項目と
突き合わせて見つかった不便な点を書き出した。

`61. しおり項目への移動手段メモ欄の追加`は完了済み(詳細はdocs/roadmap-done.md参照)。
`62. 概要タブの集合情報への地図リンク追加`は完了済み(詳細はdocs/roadmap-done.md参照)。
`63. 日程調整の○×△内訳サマリー`は完了済み(詳細はdocs/roadmap-done.md参照)。
(`64`. 宿泊候補と確定宿泊のつながりの追跡は、`56`(候補→確定へのワンタップ変換)と
セットで完了済み。詳細はdocs/roadmap-done.md参照)

`65. メモ欄のURLリンク化`は完了済み(詳細はdocs/roadmap-done.md参照)。

以下は要件(`docs/requirements.md`)の既存の確定事項と衝突する、または設計上の
トレードオフが大きいため、タスク化はせず検討事項として記録するに留める。
- 雑多メモから「→行き先決めへ」振り分けた際、選択範囲の全文がそのまま候補地の
  `name`に入る(例:「今度沖縄行くなら絶対美ら海水族館行きたい」を選択すると、
  候補地名がその全文になってしまい、本来「美ら海水族館」だけが名前で残りは`note`に
  分かれてほしい場面がありそう)。ただし文からどこまでが「名前」かを自動判定するのは
  誤爆しやすく仕様設計が難しいため、対応要否は人間と要相談
- 宿泊候補は`docs/requirements.md`7-3で「投票機能を持たない」と明確に決まっている
  (決定は口頭・Discord等)。実際に使うと絞り込みの補助が何も無く不便に感じる場面が
  ありうるが、要件のWon't判断を覆すことになるため、対応するなら要件変更として
  人間の判断を仰ぐ必要がある
- `65`と同じ「メモ欄のURLがリンクにならない」問題は、雑多メモ(`scratchText`)・
  企画メモ(`planningNotesText`)にも当てはまる(貼ったURLがリンクとして機能しない)。
  同様に、`46`(先頭が「#」「・」等の行を表示側だけ軽く強調する簡易装飾案)も
  この2つには適用できないと判明した。理由は共通で、この2つは常時編集可能な
  `<textarea>`で表示しており、`textarea`の中身は構造上プレーンテキストしか
  表示できないため、「表示だけを装飾/リンク化する」方法が使えない。実現するには
  「閲覧モード(リンクが効く・装飾された整形済み表示)⇔編集モード(textarea)」を
  切り替える設計変更が必要になり、現状の「常時編集・自動保存」というシンプルさとの
  トレードオフが生じるため、対応するかどうかは人間と相談してから方針を決める
  (対応する場合、`46`・URLリンク化の両方をまとめて解決できる可能性が高い)

上記の検討事項(要件との衝突・トレードオフのため人間の判断待ち)を除き、
第11期のタスクは以上で全て完了。

---

## 第12期: 外部レビューを踏まえたデザイン改善

2026-08-20、人間が二人のレビュアーに現在のビジュアルデザインを見てもらい、
そのコメントを共有してもらった。全体トーンは好評だったが、「しおり・旅の
あたたかみ」というコンセプトと実装(色の後付け感・SaaSダッシュボード的な
質感)がズレているという指摘が中心。人間と口頭ですり合わせて対応方針を
確定した項目のみ、以下タスク化する。

`66. 色数整理(accent-green廃止)`・`67. 見出し用ディスプレイフォントの追加`・
`68. しおりモチーフの追加`は実装完了(詳細はdocs/roadmap-done.md参照)。`68`は
やりすぎるとチープに見えるリスクがあるとして「実装後は特に人間の目で確認する
こと」と注記されていたタスクのため、evolveサイクルでのPlaywright確認・
スクリーンショット目視に加えて、**人間による実機/実画面での最終確認が
まだ済んでいない**。違和感があれば調整・取り消しのフィードバックを歓迎する。
`69. カードシャドウの調整`・`70. ヘッダー・暗色カードのドット柄廃止(切手風
パーフォレーション採用)`は完了済み(詳細はdocs/roadmap-done.md参照)。
`71. ボタン・カードの押下アニメーションに物理的な質感`は完了済み(詳細は
docs/roadmap-done.md参照)。

2026-08-20、レイアウト構造の作り込み(横スクロールでページをめくる感覚・余白の
非対称性)について人間と会話し、「タブバー・ルーティング・各タブの情報設計
(`docs/screens.md`)は変えず、装飾・演出のみで『しおりらしい構造』を感じさせる」
という方向(=解釈Aの拡張)で具体化した。ナビゲーションの仕組み自体を変える
「解釈B」(横スワイプ操作への置き換え等)は見送り、この案で確定。

`72. しおりらしい構造演出3点セット`は実装完了(詳細はdocs/roadmap-done.md参照)。
`71`と同様、実装後は人間の目での確認を挟むこととされていたタスクのため、
evolveサイクルでのPlaywright確認・スクリーンショット目視に加えて、
**人間による実機/実画面での最終確認がまだ済んでいない**。違和感があれば
調整・取り消しのフィードバックを歓迎する。

`73. ページめくりトランジション中の固定バー消失を修正`は実装完了(詳細は
docs/roadmap-done.md参照)。

上記`73`の完了により、第12期(外部レビューを踏まえたデザイン改善)のタスクは
以上で全て完了。

---

## 第13期: 実機/実画面フィードバック対応(その4)

2026-08-21、`68`(しおりモチーフ)・`72`(しおりらしい構造演出)・`44`(雑多メモ
固定バー)・`58`(しおりの並び替え)について、人間が実機/実画面で操作した上での
フィードバック。

### 装飾・見た目
`74. パーフォレーション廃止(左端の綴じ穴のみ残す)`・
`75. しおりタイムラインバッジの過去/次/未来3アイコン化`は完了済み(詳細は
docs/roadmap-done.md参照)。
`80. しおりタイムライン連結線をランダムな足あとの小道に変更`は完了済み
(詳細はdocs/roadmap-done.md参照)。
`77. ボタンの角丸・太さの見直し(ピル型統一)`は完了済み(詳細は
docs/roadmap-done.md参照)。
`83. 濃紺グラデーションの角度を役割ごとに変えて使い回し感を解消`は完了済み
(詳細はdocs/roadmap-done.md参照)。

第13期の「装飾・見た目」サブセクションはこれで全て完了。

### 操作フィードバック
`78. しおり並び替え後のハイライト点滅を追加`は完了済み(詳細は
docs/roadmap-done.md参照)。

### レイアウト
`79. ヘッダー/サイドバーの構成見直し(アプリ名・戻るリンクをサイドバーへ、
ヘッダーは旅行名を表示)`は完了済み(詳細はdocs/roadmap-done.md参照)。

これで第13期(実機/実画面フィードバック対応(その4))の全サブセクション
(装飾・見た目/操作フィードバック/レイアウト)が完了。
